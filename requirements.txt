# Sports Analysis — unified dependencies
# Union of basketball_analysis/requirements.txt and
# football_analysis/requirements.txt. Where both pinned a package, the
# stricter (basketball) pin is kept — it satisfies football's minimums.

# ── app ──────────────────────────────────────────────────────────────────────
streamlit>=1.30
altair>=5.0

# ── core numerics ────────────────────────────────────────────────────────────
# torch 2.2.x is built against the NumPy 1.x C API — with numpy>=2 every
# torch<->numpy conversion raises "Numpy is not available". Keep <2.0
# unless torch is upgraded too.
numpy>=1.26,<2.0
pandas==2.2.3
scipy
scikit-learn>=1.3.0
Pillow==11.1.0

# ── vision / tracking ────────────────────────────────────────────────────────
opencv-python-headless==4.10.0.84
ultralytics==8.3.67
supervision==0.25.1
torch==2.2.0
transformers==4.46.3          # Fashion-CLIP team assignment (basketball)

# ── video encoding ───────────────────────────────────────────────────────────
av

# ── plotting (football heatmaps) ─────────────────────────────────────────────
matplotlib

# ── model fetching ───────────────────────────────────────────────────────────
gdown>=5.1.0
roboflow==1.1.51

# ── optional: Intel GPU / CPU acceleration ───────────────────────────────────
# Unlocks the "OpenVINO — Intel GPU" backend in the basketball Performance
# panel. Measured ~5x faster per-model than PyTorch CPU on an Intel iGPU.
# Safe to omit; the app falls back to PyTorch and hides the option.
openvino>=2024.0
onnx
